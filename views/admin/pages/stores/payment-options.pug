extends ../../layout-list

block list
  .remark
    b A
    | #{' '} = Activo,
    | #{' '}
  table.small.table.table--lines.table--lines.table--striped
    thead
      tr
        th.b.tc A
        th.w-100.b Metodo de Pago
    tbody
      each item of payments
        tr
          td.tc
            +boolean(item.active)
          td.w-100
            .b #{item.name}
            .small #{item.description}