extends /modules/common/views/layout-index

block view
  script(type='application/ld+json').
    {
      "@context": "http://www.schema.org",
      "@type": "LocalBusiness",
      "name": "#{_.get(req, 'site.name')}",
      "url": "#{req.site.url}",
      "sameAs": !{JSON.stringify(_.get(req, 'site.sameAs'))},
      "logo": "#{appCnf.url.cdn}tenancy/#{req.tenancy}/images/logo-01.png",
      "image": "#{appCnf.url.cdn}tenancy/#{req.tenancy}/images/logo-02.png",
      "description": "#{_.get(req, 'site.description')}",
      "address": {
        "@type": "PostalAddress",
        "addressCountry": "Colombia"
      }
    }
  | !{item.html}