extends /modules/admin/views/super/layout-edit-cmp

block edit
  form.validate-rest(
    id="updateProfileForm"
    action=`${appCnf.url.api}admin/super/emails-templates/${item._id}`
    method="put"
    data-page=`/administracion/super/emails-plantillas`
    data-error="#form-results")
    .white.br-1.rd-0-2.p-3
      .msg.error#form-results
      .row
        .col-xs-12
          .form-group
            .form-control
              input.input#name(type="text" name="name" placeholder=' ' required value=item.name)
              label(for='name') Nombre
          .form-group
            .form-control
              input.input#slug(type="text" name="slug" placeholder=' ' value=item.slug)
              label(for='slug') Slug
          .form-group
            .form-control
              input.input#subject(type="text" name="subject" placeholder=' ' required value=item.subject)
              label(for='subject') Sujeto
          .form-group
            .form-control
              textarea.input#description(name="description" placeholder=' ') #{item.description}
              label(for='description') Descripción
          .form-group.ph-1
            .subtitle.mb-0-5 Texo
            .form-control
              textarea.cke-admin#text(name="text" placeholder=' ') #{item.text}
      .row
        .col-xs-12.tr
          button.btn.btn--primary.w-100-xs.mt-1-xs(type="submit") Guardar Plantilla
