extends /modules/common/views/layout-index

block view
  .container
    .row.pb-2
      .col-xs-12
        h1.tc #{title}

    .row.justify-content-center
      .col-md-8
        .msg.mb-2(class=msg.color)
          if msg.title
            .b.title.p-1 #{msg.title}
          .hr
          .p-1 !{msg.text}

